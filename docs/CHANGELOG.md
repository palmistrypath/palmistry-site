# Changelog - Palmistry Path

Meaningful project-state changes only; Git history remains the detailed implementation record.

## 2026-08-22 - Legacy Head Line pillar, revision 2: residual prevalence/consensus and anonymous-authority cleanup (Relay PP-RELAY-042, revision 2)
- The revision-1 PR (#66) was closed without merging: Director review found it had removed several risky claims but incorrectly retained multiple unsupported prevalence/consensus and anonymous-authority statements within this task's cleanup categories. This revision reconstructs the sound revision-1 corrections and adds the required remediation on top of them in a single pass against current `main`, since none of revision 1's edits had reached `main`.
- Preserved revision-1 corrections: removed the "consistent Western formulation" consensus claim, the "generally the most positively read position" ranking claim, the "other writers confirm"/"the wider... the more" degree-scaling pair, the "classical writers"/"classical texts"/"Contemporary writers have reframed this" anonymous-authority instances on the starting-point readings, the direction/path section's monotonic degree-scaling sentences, and Cheiro's unauthorized "extreme morbid imagination" clinical framing — all within the Director-verified Benham evidence boundary at `.ai-ops/evidence/PP-RELAY-041-benham-head-line-direction.md`.
- New revision-2 corrections: removed unattributed "typically"/"usually" prevalence wording on the head line's start location and origin relative to the life line; removed the "discusses most specifically"/"the tradition is precise" ranking claim on the starting-point section's intro; reworded the writer's-fork naming sentence to state, on the already-verified evidence in the sibling `forked-head-line-meaning.md` article, that the term is a later popular-palmistry label neither Cheiro nor Benham uses (removing "among the most discussed" and "commonly called"); removed an unverifiable quotation anonymously attributed to "one contemporary practitioner" together with the "notable frequency"/"which is how the name spread" prevalence and causal-history claims, restating the same corrective point in the article's own editorial voice; removed "generally" from the upward-branches reading; removed "uncommon" from the double-head-line description; removed the FAQ's "typically" on the shared origin with the life line and the myth section's "appears frequently in writers' hands"; and reframed the closing myth's "not how the contemporary tradition works" as the article's own stated position rather than an anonymous-consensus claim.
- No new palmistry meaning, prevalence claim, scientific claim, combination reading, or clinical/medical language was introduced; every other interpretation in the article was reviewed and left unchanged.
- Full claim-by-claim disposition in `docs/source-verification-log.md`.
- Verified with `npm run audit:claim-risk -- src/content/blog/beginner/head-line.md` (22 → 14 findings, all remaining dispositioned as compliant), `npm run build` (110 pages), `npm run content-audit` (65 blog posts, 25 lessons valid), `npm run audit:all` (links — including the no-orphan check — images, schema, indexability, accessibility all pass), and `git diff --check`.

## 2026-08-22 - Straight vs Curved Head Line companion article, revision 4: residual claim-boundary corrections (Relay PP-RELAY-041, revision 4)
- Cumulative Director review of revision 3 (PR #64, never merged to `main`) found two residual source-boundary issues: the "Path in context" section attributed head-line depth/clarity to "focus and concentration," a meaning the approved Benham evidence note does not establish, and the "What it's traditionally associated with" section described Benham's reading of direction as "one of its more telling features," a historical embellishment not directly established by the evidence note.
- Removed the unsupported depth/clarity meaning; the article now states depth/clarity is a separate feature covered in the main head line guide, without assigning it an interpretation here. Narrowed the historical framing to state Benham "associated the direction of the head line with different mental tendencies," without ranking direction among the line's more or less telling features.
- No other revision-3 wording was reopened; the accepted straight/practical and sloping/imagination evidence boundary, the corrected myths, and the FAQ content are unchanged.
- Full claim-by-claim source disposition in `docs/source-verification-log.md` ("...revision 4").
- Verified with `npm run audit:claim-risk -- src/content/blog/beginner/straight-head-line-palmistry.md` (0 findings), `npm run build` (110 pages), `npm run content-audit` (65 blog posts, 25 lessons valid), `npm run audit:all` (links — including the no-orphan check — images, schema, indexability, accessibility all pass), and `git diff --check`.

## 2026-08-22 - Straight vs Curved Head Line companion article published within a Director-verified Benham evidence boundary (Relay PP-RELAY-041, revision 3)
- Added `src/content/blog/beginner/straight-head-line-palmistry.md`, item 13 from `docs/editorial-backlog.md`'s Next 25 list. Revision 1 had drafted the article using the canonical Head Line lesson's prose as its evidentiary basis rather than an independently verified source, and revision 2 returned `HUMAN_REQUIRED` when this worker's environment could not reach the required public-domain Benham text; neither revision's draft was ever merged, so no article file existed on `main` before this task. The Director independently verified William G. Benham, *The Laws of Scientific Hand Reading* (1900), and recorded the evidence boundary at `.ai-ops/evidence/PP-RELAY-041-benham-head-line-direction.md`.
- The article covers only what that evidence note supports: a straight-running head line attributed to Benham as practical mentality, and a head line sloping toward the Mount of Luna attributed to Benham as imagination and a departure from purely material thought — both kept narrow and non-deterministic. Benham's writer/poet examples among sloping lines are mentioned as historical examples only; the article explicitly does not state or imply a monotonic "steeper slope = more creative" rule, and names that exact claim as a corrected myth. No fixed angle/zone/average cutoff, prevalence/consensus language, anonymous authority, combination reading, or clinical/insanity language was introduced.
- `docs/editorial-backlog.md`'s item 13 is marked published, its slug corrected from the originally-scored `straight-curved-head-line` to the task-packet-specified `straight-head-line-palmistry`, and the mechanical published count updated 64 → 65.
- Full claim-by-claim source disposition in `docs/source-verification-log.md`, including a noted-but-out-of-scope observation that `blog/beginner/head-line.md` (a separate file from the cleaned-up canonical lesson) still carries unsupported monotonic degree-scaling wording for head-line slope, left untouched per this task's bounded scope.
- Verified with `npm run audit:claim-risk -- src/content/blog/beginner/straight-head-line-palmistry.md` (0 findings after drafting-stage rewording), `npm run build` (110 pages), `npm run content-audit` (65 blog posts, 25 lessons valid), `npm run audit:all` (links — including the no-orphan check — images, schema, indexability, accessibility all pass), and `git diff --check`. Full source-claim preflight disposition recorded in the task's durable result artifact.

## 2026-08-22 - Canonical Head Line lesson, revision 2: residual claim-boundary corrections (Relay PP-RELAY-040 r2)
- Director review of the revision-1 PR (#62) found four residual passages that, while no longer matching the automated heuristic's word list, still exceeded the approved evidence boundary: an unattributed cross-tradition consistency claim for path/slope, an unsupported "normal middle case" distribution claim for slope, a universalized endpoint-location claim contradicted by the lesson's own Length section, and a vague "popular palmistry gets wrong" anonymous-authority attribution.
- Corrected all four in `src/content/lessons/lines/03-head-line.mdx` without reopening any other reviewed revision-1 wording; none was replaced with an equivalent unsupported synonym (`normal`, `usual`, `standard`, `generally`, `consistently`).
- Full claim-by-claim disposition in `docs/source-verification-log.md` ("...revision 2").
- Verified with `npm run audit:claim-risk -- src/content/lessons/lines/03-head-line.mdx` (same 9 findings as rev. 1, all previously dispositioned; none of the four edits introduces a new finding), `npm run build` (109 pages), `npm run content-audit`, `npm run audit:all`, and `git diff --check`.

## 2026-08-22 - Canonical Head Line lesson: source-integrity claim-boundary cleanup (Relay PP-RELAY-040)
- Bounded SOURCE_SENSITIVE cleanup of `src/content/lessons/lines/03-head-line.mdx`, closing the `central zone` fixed-cutoff finding PP-RELAY-037/PP-RELAY-039 had already flagged as unresolved on `main`, plus the wider prevalence/ranking and monotonic degree-extrapolation wording the task packet asked to be independently re-swept.
- Removed the unsupported `ending before it reaches the central zone of the palm` fixed geometric threshold, replacing it with the same palm-relative comparative framing already vetted in `beginner/short-head-line-meaning.md` (PP-RELAY-037 rev. 3).
- Removed four monotonic degree-extrapolation sentences: two matched by the `PRECISION_OR_DEGREE_EXTRAPOLATION` heuristic (`the longer... the more` on line origin; the `central zone` cutoff) and two the heuristic's word-list misses because they use "further" rather than "longer/shorter/deeper/...": `the further the slope, the more strongly...` and `the further it descends into Luna, the stronger...`. Flagged for a tooling follow-up (heuristic word-list gap), not fixed in this content-only task.
- Narrowed nine unattributed prevalence/ranking claims with no repository-held evidence (physical prevalence across hands, and unattributed consensus/ranking claims about how the tradition reads a feature) to observational or attributed framing; one further unflagged ranking claim (`appears consistently as a positive marking in the Western sources`) was removed on the same independent-review pass. Full claim-by-claim disposition, including the claims left unchanged as compliant or already-verified, is in `docs/source-verification-log.md`.
- No new palmistry meaning, prevalence claim, scientific claim, or combination reading was introduced. Lesson structure, figures, and learning flow are unchanged; only prose within existing sections was narrowed.
- Verified with `npm run audit:claim-risk -- src/content/lessons/lines/03-head-line.mdx` (22 findings → 9, all remaining findings dispositioned as compliant), `npm run build` (109 pages), `npm run content-audit` (64 blog posts, 25 lessons valid), `npm run audit:all` (links, images, schema, indexability, accessibility all pass), and `git diff --check`.

## 2026-08-22 - Claim-risk preflight: added precision/degree-extrapolation category (Relay PP-RELAY-039)
- Added a new `PRECISION_OR_DEGREE_EXTRAPOLATION` heuristic category to `scripts/audit-claim-risk.mjs`, tied to the specific unsupported-precision patterns that caused avoidable rework on `beginner/short-head-line-meaning.md` (PP-RELAY-037, an invented `central zone` cutoff) and `beginner/long-head-line-meaning.md` (PP-RELAY-038, `farther than average` and `a very long line indicates...` degree scaling): average-based comparative cutoffs (`farther/longer/shorter... than average`), fixed anatomical-zone cutoff phrasing (`ending before`/`reaches... the central zone`), and monotonic degree-scaling wording (`very long... indicates`, `the longer... the more`, `the shorter... the less`).
- Added `scripts/fixtures/claim-risk/risky-precision-degree.md` (positive fixture covering all three sub-patterns) and extended `scripts/fixtures/claim-risk/neutral-observational.md` with the packet's example neutral comparative sentence (`compare how far the line travels across this palm`) to confirm the new category does not flag ordinary comparative observation. `npm run audit:claim-risk:selftest` covers both.
- Existing categories (`PREVALENCE_OR_CONSENSUS`, `ANONYMOUS_AUTHORITY`, `SCIENTIFIC_OR_HISTORICAL_OVERSTATEMENT`) and CLI behavior are unchanged. The new category is not wired into `audit:all` or any dispatch/merge/fast-lane behavior, consistent with the existing targeted-preflight-only scope.
- Spot-checked the new category against every existing published blog/lesson file: 3 findings across 89 files, including the still-unresolved `central zone` cutoff already known from PP-RELAY-037 in `lessons/lines/03-head-line.mdx` (line 64) and two `the wider/longer... the more` degree-scaling instances in `blog/beginner/head-line.md` and `lessons/mounts/05-mount-of-apollo.mdx`. These are heuristic review prompts for future SOURCE_SENSITIVE work, not proof of a defect; no published content was changed by this task.
- Verified with `npm run audit:claim-risk:selftest` (all fixtures pass), `npm run build` (109 pages built), `npm run content-audit` (64 blog posts, 25 lessons valid), and `git diff --check`.

## 2026-08-22 - Via Lascivia article, revision 3: residual translation/etymology claim removed (Relay PP-RELAY-036)
- Added `src/content/blog/beginner/via-lascivia-palmistry.md`, the last remaining unpublished item from `docs/editorial-backlog.md`'s Next 10 list, sourced from the Director-verified evidence boundary at `.ai-ops/evidence/PP-RELAY-036-benham-via-lascivia.md`: William Benham, *The Laws of Scientific Hand Reading* (1900), Part Second, Chapter XIV, "The Minor Lines," section "The Via Lascivia." Gettings (1965) was not independently verified for this task and was not used.
- Revision 2 (PR #56, unmerged) had returned `READY_FOR_REVIEW` with three earlier revision-1 source-boundary overstatements already corrected (tradition-wide naming attribution narrowed to Benham; a modern prevalence/universality sentence removed; a comparative-strength phrase beyond Benham's own qualification removed). Cumulative Director review of PR #56 then found one residual claim outside the controlling evidence boundary: the historical-name section's `roughly, a path of excess or wanton way`, an unverified literal/approximate Latin translation of "Via Lascivia" that the approved evidence does not independently establish.
- Revision 3 removes exactly that phrase and replaces it with non-translation wording that stays inside the evidence note — Benham connects the historical name to an older reading tied to ideas of surplus energy and excess, explicitly not a verified translation of the Latin name. All revision-2 corrections are preserved unchanged, and the rest of the article was manually re-inspected against the full source-sensitive claim-risk category list (prevalence, scientific/historical, invented combinations, vague attribution, quotation fidelity, safety boundaries) with no further change needed.
- `docs/editorial-backlog.md`'s item 8 remains marked published with the mechanical count at 62; `docs/CURRENT_STATE.md`'s blog-post-count references were updated to match. No reciprocal link was added into `mount-of-luna.md`, `mercury-line.md`, or `minor-lines-overview.md`; the article links out to all three, consistent with revision 2.
- Verified with `npm run build` (107 pages built), `npm run content-audit` (62 blog posts, 25 lessons valid), `npm run audit:all` (links — including the no-orphan check — images, schema, indexability, accessibility all pass), `npm run audit:claim-risk` against the changed file (0 findings), and `git diff --check`. Full source-claim preflight disposition recorded in the task's durable result artifact.

## 2026-08-21 - Ring of Solomon article published within a Cheiro + Benham evidence boundary (Relay PP-RELAY-035, revision 3)
- Added `src/content/blog/beginner/ring-of-solomon-palmistry.md`, the last unpublished item from `docs/editorial-backlog.md`'s Next 10 list. A Cheiro-only revision 2 had correctly returned `HUMAN_REQUIRED`: the single source didn't support a detailed geometric description and left the article shape too thin. The Director independently verified a second classical source — William Benham, *The Laws of Scientific Hand Reading* (1900), Chapter XIV, "The Minor Lines," section "The Ring of Solomon" — recorded at `.ai-ops/evidence/PP-RELAY-035-benham-ring-of-solomon.md`, alongside the previously verified `.ai-ops/evidence/PP-RELAY-035-cheiro-ring-of-solomon.md`, and authorized revision 3 to draft within the combined boundary.
- The article covers only what the two verified sources support: location/geometry (Benham's specific path around the Mount of Jupiter; Cheiro's less detailed placement on the same mount, illustrated as mark 8 on Plate XX), the narrow mysticism/occult-interest theme both sources independently associate with the marking, and Benham's own conditional proficiency reading (dependent on unspecified other chirognomic indications, which the article does not invent). Gettings (1965) remains unverified and was not used, though the backlog's original notes referenced him.
- No quotation marks were used for Cheiro/Benham content — the evidence notes were not independently re-verified verbatim, so all source content is paraphrased, per the task's source-sensitive constraints. No prevalence/rarity claim was made about the marking; the FAQ explicitly states the two sources don't address frequency rather than guessing at "rare" or "common." No combination reading beyond what each source states on its own was introduced, and no anonymous "modern palmists"/"some writers" attribution was used.
- `docs/editorial-backlog.md`'s item 9 is marked published and its mechanical count updated 60 → 61; `docs/CURRENT_STATE.md`'s two blog-post-count references were updated to match. No reciprocal link was added to `mount-of-jupiter.md` or `minor-lines-overview.md`, since neither article's existing structure has an uncontrived slot for one without a broader rewrite outside this task's authorized scope; the new article links out to both.
- Verified with `npm run build` (106 pages built), `npm run content-audit` (61 blog posts, 25 lessons valid), `npm run audit:all` (links — including the no-orphan check — images, schema, indexability, accessibility all pass), `npm run audit:claim-risk` against the changed file (9 findings before manual disposition and rewording, 2 after — both the "rare or common" FAQ question and its explicit non-claim disclaimer, retained as the required safe pattern), and `git diff --check`. Full source-claim preflight disposition recorded in the task's durable result artifact.

## 2026-08-21 - Minor-lines-overview empirical/anatomical claim audit, revision 2 (Relay PP-RELAY-034)
- Director review of the revision-1 diff below found two of its own corrections still exceeded approved evidence: the corrected fetal-development sentence added an unsupported "essentially every hand" prevalence inference, and the corrected surface-marking sentence still listed "the ordinary effects of aging" as a causal example.
- Removed the "essentially every hand" prevalence clause; the fetal-development sentence now ends at the developmental-timing fact the Kimura & Kitagawa (1986) evidence supports, with no prevalence conclusion drawn from it.
- Removed "the ordinary effects of aging" from the surface-marking examples, leaving "general skin texture and incidental creasing" with no causal attribution.
- Reworded "The fate line and the minor lines are less fixed in that way" to "...are not among those three principal creases" — a plain classificatory statement instead of an implied comparative-stability claim tied to the fetal-development mechanism — while leaving unchanged the already-acceptable observation that their presence and clarity vary far more from person to person.
- No new palmistry meaning, combination reading, prevalence estimate, or vague anonymous attribution was introduced. `npm run audit:claim-risk` on the file still returns the same 2 findings (lines 13, 34), both already dispositioned as non-empirical. Full per-claim disposition in `docs/source-verification-log.md`.

## 2026-08-21 - Minor-lines-overview empirical/anatomical claim audit (Relay PP-RELAY-034)
- Manually inventoried every empirical/anatomical/developmental/prevalence claim in the opening "What minor lines are" and "Why some hands don't have them" sections of `src/content/lessons/advanced/01-minor-lines-overview.mdx`, including claims the `audit:claim-risk` heuristic does not flag (it pattern-matches prevalence vocabulary, not anatomical/developmental assertions).
- Narrowed the fetal-development claim to what the repo's own Kimura & Kitagawa (1986) evidence actually supports: only the heart, head, and life lines are the palm's three principal flexion creases formed before fetal hand movement begins. The fate line, previously grouped into that anatomical claim, is now grouped with the minor lines as "less fixed," with no anatomical-origin claim asserted about it either way.
- Removed the unsupported claim that minor lines are shaped by "individual constitution, habitual patterns of movement" as a physical mechanism; the repository's only comparable language frames this as a traditional/interpretive reading, not an anatomical cause.
- Removed the ungrounded "a clear heart line appears on virtually every adult hand" prevalence/legibility statistic, since the point it made is already covered by the corrected, evidence-grounded fetal-development sentence one paragraph earlier.
- Reworded "well within the normal range of human hands" (no repository-held statistical baseline) to the site's own framing that hand variation is not a deficiency, without borrowing unsupported statistical language.
- Reworded the claim that habitual movement and aging "produce" specific palm markings (no repository evidence for an adult-line-formation mechanism) to state only that such surface markings can appear without being a minor line in the interpretive sense — preserving the practical reading guidance without the unsupported causal claim.
- No new palmistry meaning, combination reading, prevalence estimate, or vague anonymous attribution was introduced to fill any removed wording. The two previously-dispositioned non-empirical scanner findings (lines 13 and 34) were re-checked and remain correctly retained.
- Verified with `npm run build` (105 pages), `npm run content-audit` (60 blog posts, 25 lessons valid), `npm run audit:all` (links, images, schema, indexability, accessibility all pass), `npm run audit:claim-risk` against the changed file (2 findings before and after, both re-dispositioned as non-empirical), and `git diff --check`. Full per-claim inventory and disposition in `docs/source-verification-log.md`.

## 2026-08-20 - Remaining minor-lines-overview claim-risk findings dispositioned (Relay PP-RELAY-033)
- Closed the 7 claim-risk findings recorded as out-of-scope under PP-RELAY-031/032 in `src/content/lessons/advanced/01-minor-lines-overview.mdx` (lines 13, 34, 38, 61), dispositioning each individually against approved repository evidence rather than assuming a scanner match was a defect.
- Retained unchanged (lines 13, 34): the curriculum's own "major lines and mounts carry the most interpretive weight" framing and the "generally set aside" surface-noise guidance are both Palmistry Path's own pedagogical/procedural statements, not external prevalence or consensus claims — the latter is directly consistent with the parallel guidance already published in `advanced/02-marriage-relationship-lines.mdx`.
- Corrected (line 38): "how its variations are typically read" → "traditionally read", matching the lesson's own established "traditionally associated with" diction and removing ambiguity about what a "typically" claim would need to rest on.
- Corrected (line 61): removed the unsupported "among the most frequently asked-about features in palmistry and among the most commonly misread" popularity-ranking claim outright (no repository evidence ranks palmistry features against each other this way). Kept and reworded the substantive point that a specific popular belief — that line count indicates relationship count and line length indicates duration — is contested, grounding it directly in `advanced/02-marriage-relationship-lines.mdx`'s existing sourced treatment (Cheiro's popularizing 1916 framing, Fincham's and Gettings' explicit contesting of the counting claim, and West's 1998 length-as-depth-not-duration framing).
- No new minor-line topic, combination reading, prevalence estimate, or curriculum scope was added; no vague anonymous-authority phrasing was substituted.
- Verified with `npm run build` (105 pages), `npm run content-audit` (60 blog posts, 25 lessons valid), `npm run audit:all` (links, images, schema, indexability, accessibility all pass), `npm run audit:claim-risk` against the changed file (7 findings before, 2 after, both retained with direct reasoning above), and `git diff --check`. Full per-finding disposition in `docs/source-verification-log.md`.

## 2026-08-20 - General minor-lines Benham attribution corrected (Relay PP-RELAY-032)
- Closed the adjacent source-integrity defect PP-RELAY-031 identified but left out of its Sun/Mercury-only scope: `src/content/lessons/advanced/01-minor-lines-overview.mdx`'s "Why some hands don't have them" section attributed to Benham the reading that absent minor lines on an otherwise well-formed hand should not be read as a deficiency. `docs/source-verification-log.md` establishes twice — for the Mercury/health line and for the Sun Line — that Benham does not read line absence at all, so the attribution had no repository-held support.
- The sentence is restated as Palmistry Path's own editorial framing ("This site does not read the absence of minor lines on an otherwise well-formed hand as a deficiency") rather than attributed to Benham. No replacement historical meaning was invented and no vague anonymous authority ("modern palmists", "some writers") was substituted, per this task's explicit constraints and the editorial-style-guide §5.2 rule that the site's own position should be stated as its own.
- Checked the immediately surrounding paragraph for an equivalent unsupported attribution, prevalence, or consensus claim; none was found — the following paragraph describes hand variation directly rather than attributing a reading to any authority.
- Verified with `npm run build` (105 pages), `npm run content-audit` (60 blog posts, 25 lessons valid), `npm run audit:all` (links, images, schema, indexability, accessibility all pass), `npm run audit:claim-risk` against the changed file (7 findings, unchanged from PP-RELAY-031's post-correction count and none at the edited line), and `git diff --check`. Full per-claim disposition in `docs/source-verification-log.md`.

## 2026-08-20 - Sun/Mercury source fidelity closes Batch 3E (Relay PP-RELAY-031)
- Closed the last outstanding Batch 3E item, the Sun/Mercury source-fidelity issue in `src/content/lessons/advanced/01-minor-lines-overview.mdx`. The quote-fidelity concern in `docs/audits/CURRICULUM_WAVE_3E_3F_BRANCH_AUDIT_2026-08.md` open item 1 was raised against the **unmerged** `feat/curriculum-wave-3e-3f` branch's proposed treatment, which added Cheiro/Benham quotations the audit could not check against the 1916/1900 editions. That treatment was never replayed onto `main`; the published lesson has no quotation marks in its body at all, so there was no quotation to verify or convert. The stale branch was not merged, rebased, or copied.
- Corrected three unsupported source claims found by reading the focused Sun/Mercury material against the site's own Batch 2B/2C source-verified `blog/beginner/sun-line.md` and `blog/beginner/mercury-line.md`: the sun line's "appears on roughly half of hands" prevalence figure (no repository evidence states any prevalence figure for any minor line); "what Cheiro described as personal magnetism" (absent from the verified Cheiro material, which supports luck, brilliancy in art/stage/public singing, and the world's recognition); and a Fincham attribution for a confidence/purposeful-direction reading that no repository evidence supports and that also inverted the site's own distinction, which assigns purposeful direction to the fate line. Fincham (2005) is in copyright and unsearchable, consistent with the verification log's standing position on in-copyright modern sources.
- Replaced the Mercury paragraph's anonymous "Some… Others…" attribution with the named readings the evidence carries — Benham for digestion, liver action, vitality, and constitution on a line that is present; the mount of Mercury for the communication, commercial-intelligence, and adaptability reading; Gettings treating the two as complementary — and removed its unsupported "most variable… genuine disagreement across sources" framing, relocating the disagreement to where the evidence places it: Cheiro reads an absent mercury line as favourable, Benham does not read the line's absence at all. The origin sentence's "often from near the mount of Luna" frequency claim was replaced with the three documented origins, unranked.
- No quotation marks were added to the lesson body; every named-source statement is a paraphrase, per the editorial-style-guide §5 rule. No new palmistry meaning or combination reading was introduced. Internet Archive, chestofbooks.com, iapsop.com, Wikisource, and Project Gutenberg are all blocked by this environment's egress proxy, so no claim rests on external text and search-engine snippets were not treated as source-quality evidence. Full per-claim evidence and disposition recorded in `docs/source-verification-log.md`.
- Deliberately left unchanged and recorded for a future bounded task: the same lesson attributes to Benham the reading that absent minor lines should not be read as a deficiency, which the verification log establishes twice that Benham does not do. It is general minor-lines prose, outside this task's Sun/Mercury-only authorized scope.
- Verified with `npm run build`, `npm run content-audit`, `npm run audit:all`, `npm run audit:claim-risk` against the changed file (9 findings before, 7 after; both Sun/Mercury findings resolved, the 7 remaining all outside the authorized scope), and `git diff --check`. Note that the scanner never flagged the "roughly half of hands" figure, the clearest unsupported empirical claim in the focused material — a concrete instance of the standing warning that a clean heuristic scan is not evidence of sourcing.

## 2026-08-20 - Curriculum inventory/status reconciliation in CURRENT_STATE (Relay PP-RELAY-030)
- Re-ran a mechanical curriculum inventory against `main` and reconciled `docs/CURRENT_STATE.md` with it. Observed state: 25 lessons across four modules — Foundations 5, Lines 6, Mounts 8, Advanced 6 — confirmed by `npm run content-audit` (60 blog posts, 25 lessons). Per-module counts are now stated explicitly alongside the module table, and `Last repo inventory` moved to 2026-08-20.
- Corrected the stale `content-audit` blog figure in the quality-tooling section from 56 posts to 60, matching the audit's actual output and the 60-post figure already used elsewhere in the same document.
- Converted five present-tense "remain deliberately out of scope" tails in the dated PP-RELAY-008/009/010/011/012 rollout paragraphs to past-tense per-task scope statements. Each paragraph is a historical record of what that task did and did not touch; the tails now say what was out of scope *for that task* rather than asserting current status, since PP-RELAY-029 has since closed the capstone body revision and PP-RELAY-010–012 shipped the module Checkpoints. No historical fact was rewritten.
- Added one current-status paragraph recording verified `<Practice>` and `<Checkpoint>` coverage, so current component state is readable without reconstructing it from the task-by-task narrative.
- `docs/AI_HANDOFF.md`'s forward-looking "What remains of 3F is narrower: the capstone body revision" now records that PP-RELAY-029 closed it, so a fresh session is not directed at completed work. The Sun/Mercury quote-fidelity rewrite in `advanced/01-minor-lines-overview.mdx` remains separate, explicitly unresolved 3E work and was not touched.
- Documentation only; no lesson, article, runtime, routing, schema, or SEO change. Verified with `npm run content-audit`, `npm run build`, `npm run audit:all`, and `git diff --check`.

## 2026-08-20 - Second worked reading closes remaining Batch 3F scope (Relay PP-RELAY-029)
- Added a second worked reading to `src/content/lessons/advanced/combining-what-you-see.mdx`, the last outstanding item from Batch 3F (`docs/ROADMAP.md`). The first worked reading demonstrates two well-supported, genuinely opposing themes held open without resolution; the second demonstrates a different synthesis problem the lesson names in its opening paragraphs — a single, visually dramatic feature (a fate line rising from the Mount of Luna on an otherwise sparsely-marked earth hand) that a reader could be tempted to promote into the reading's centre, and why it should be reported and set aside instead because nothing else on the hand corroborates it. The second reading also demonstrates the active/passive "agree → settled" case, complementing the first reading's "differ → developed through experience" case.
- Every interpretive association used is reused, not invented: the earth hand shape and the paired life-line-arc/Venus-mount reading (`lessons/foundations/03-hand-shapes.mdx`, `lessons/mounts/02-mount-of-venus.mdx`, `lessons/lines/04-life-line.mdx`), Lower Mars (`lessons/mounts/08-mount-of-mars.mdx`), and the fate line's straight course and Luna-origin association (`lessons/lines/05-fate-line.mdx`). No new palmistry interpretation, prevalence/consensus claim, or combination reading was introduced; the fate line's dramatic appearance is explicitly not folded into the hand's groundedness theme.
- Ran `npm run audit:claim-risk -- src/content/lessons/advanced/combining-what-you-see.mdx` against the changed file. It flagged 3 terms in the newly added prose (`most`/`most`/`most`, all superlatives comparing lines within the same fictional example hand, e.g. "the single most dramatic line on the hand") and 9 pre-existing terms in unchanged prose from the lesson's original PP-RELAY-008 text. All 3 new findings were manually adjudicated as within-example comparative description, not prevalence/consensus claims about palmistry generally, and required no change; the 9 pre-existing findings were left untouched as out of scope for this task.
- Updated `docs/CURRENT_STATE.md` and `docs/ROADMAP.md` to record 3F capstone-body completion; the Sun/Mercury quote-fidelity rewrite in `advanced/01-minor-lines-overview.mdx` remains separate, unresolved 3E work.
- Verified with `npm run build` (105 pages built), `npm run content-audit` (60 blog posts, 25 lessons valid), and `npm run audit:all` (links, images, schema, indexability, accessibility all pass).

## 2026-08-20 - Orphan-audit inbound links now require an indexable source (Relay PP-RELAY-027 revision 2)
- Fixed a gap found in Director review of revision 1: the orphan detector correctly required the *target* article to be indexable, but counted an inbound link from *any* built page as rescuing it, including `noindex`/private pages. `scripts/audit-links.mjs` now also requires the link's source page to be indexable (per `src/indexability.mjs`) before it counts.
- Verified with the same synthetic-orphan technique as revision 1, extended: after stripping all real inbound links to one article, adding a link to it from a `noindex` page (`/guide/thank-you/`) still failed the audit naming that route (proving a non-indexable source cannot mask an orphan), while adding a link from an indexable page made the audit pass; the build was then restored and re-verified clean. `npm run build`, `npm run audit`, `npm run audit:all`, and `npm run content-audit` all pass on final state.
- Updated `docs/CURRENT_STATE.md` with the source-indexability requirement and verification.

## 2026-08-20 - Orphaned-blog-article detection added to the link audit (Relay PP-RELAY-027)
- `scripts/audit-links.mjs` (`npm run audit`) now also fails when a published/indexable blog article route has zero meaningful inbound internal links from another indexable page's built HTML, in addition to its existing broken-internal-link check. Self-links do not count, and the `/blog/` listing page is excluded as a link source because it mechanically lists every post and would otherwise make the check unable to ever fail.
- No new dependency or standalone script; the existing per-file link scan now also builds an inbound-link graph and reports orphan routes by name.
- Verified locally: `npm run build` then `npm run audit` passes with zero orphans on current repository state (60 published posts). A controlled synthetic orphan was created by stripping all real inbound references to one article from a built `dist/` copy (a gitignored artifact, not committed); the audit failed and named the exact route, then the build was restored from backup and re-verified passing before commit. `npm run audit:all` and `npm run content-audit` also pass on final state.
- Updated `docs/CURRENT_STATE.md` and `docs/DECISIONS.md` to document the check and the blog-index exclusion rationale.

## 2026-08-20 - Published-blog-count drift check added to content-audit (Relay PP-RELAY-026)
- PP-RELAY-025 mechanically corrected `docs/editorial-backlog.md`'s documented published blog count after it had silently drifted from 53 to 60. To prevent recurrence, `scripts/audit-content.mjs` now parses the backlog's `**Published:** N blog posts` marker and blocks `npm run content-audit` when that number does not match the actual `src/content/blog` collection count, or when the marker cannot be found/parsed (fails closed rather than skipping silently).
- No new dependency, standalone script, or content change. Integrated into the existing `content-audit` check that already runs in CI (`ci.yml`) on every pull request to `main` and push to `main`, so a future drift now fails the same PR that introduces or misses a count update.
- Verified locally: normal repository state passes `npm run content-audit` (60 blog posts, 25 lessons valid); a deliberately simulated documented-count mismatch (60 → 61, not committed) produced a clear `Documented published blog count (61) does not match actual src/content/blog count (60)` failure and a non-zero exit; the backlog file was restored and `content-audit` re-verified passing before commit.
- Updated `docs/CURRENT_STATE.md` to document the new check alongside the existing lesson `order`-uniqueness description.

## 2026-08-19 - Fate Line Starting Points article published (Relay PP-RELAY-023)
- Added `src/content/blog/beginner/fate-line-starting-points.md`, closing the #10-ranked open item in `docs/editorial-backlog.md`'s "Next 10" queue. `beginner/fate-line.md` already covers the fate line broadly, including a starting-point subsection; this article targets the specific "fate line starting point meaning" search intent as a focused companion, following the same pattern as the existing `fate-line-branches-meaning.md` and `no-fate-line-meaning.md` variation articles. `cluster: "line-variations"`, `relatedLesson: "/learn/lines/05-fate-line"`, matching the sibling variation articles and the backlog's original entry.
- Covers only the four starting points approved repository evidence directly establishes — wrist/base, Mount of Luna, life line, and mid-palm/later — grounded in `lessons/lines/05-fate-line.mdx` and `blog/beginner/fate-line.md`. No additional origin was invented for completeness. The Mount of Luna section keeps three distinct readings separate rather than collapsing them into one consensus claim: Benham's historical formulation (quoted verbatim, wording already verified in the lesson) tying the starting point to assistance from someone of the opposite sex; Cheiro's separate reading of a course shaped by outside approval; and West's later, broader extension toward public or collaborative work. The "Reading the starting point in context" section reuses, rather than invents, the exact Luna/Apollo-mount/head-line combination example already established in the lesson, and explicitly cautions against combining a starting point with another feature beyond what is already established.
- Sourced from Cheiro (`Palmistry for All`, 1916), William G. Benham (`The Laws of Scientific Hand Reading`, 1900), Fred Gettings (`The Book of the Hand`, 1965), Peter West (`The Complete Illustrated Guide to Palmistry`, 1998), and Johnny Fincham (`The Spellbinding Power of Palmistry`, 2005) — the same five sources already cited for starting points in `lessons/lines/05-fate-line.mdx`, carried forward rather than re-verified against new claims since no new interpretation was added. One quotation is used (Benham's Luna formulation), matching the verbatim wording already verified in the lesson.
- Added a single reciprocal navigation link from `fate-line.md`'s existing "The starting point" subsection to the new article. No other wording in that file changed. Updated `docs/editorial-backlog.md` (published count 52 → 53, item 10 struck through and dated, detail entry marked published, Internal Link Priorities row updated) and `docs/CURRENT_STATE.md`'s blog post count (59 → 60).
- Verified with `npm run build` (105 page(s) built, including `/blog/beginner/fate-line-starting-points/`), `npm run content-audit` (60 blog posts, 25 lessons valid), `npm run audit:all` (links, images, schema, indexability, accessibility all pass), and `git diff --check`. Diff is bounded to the new article, the one reciprocal link, and the canonical docs.
- **Revision 2:** Director review of revision 1 found an unsupported prevalence claim in the FAQ answer to "Where does the fate line usually start?" — `There is no single typical starting point... not a deviation from a standard` asserted a default/prevalence position the approved sources do not establish. Rewrote the answer to describe the four starting points without asserting whether any is more or less common; a pass over the rest of the article for equivalent unsupported default/prevalence wording found none (the "common" claims about Luna and late-starting lines are directly attributed to West/Fincham and match `lessons/lines/05-fate-line.mdx`). Re-verified with `npm run build`, `npm run content-audit`, `npm run audit:all`, and `git diff --check`.

## 2026-08-19 - Short Heart Line Meaning article published (Relay PP-RELAY-022)
- Added `src/content/blog/beginner/short-heart-line-meaning.md`, closing the #7-ranked open item in `docs/editorial-backlog.md`'s "Next 10" queue. `beginner/heart-line.md` already covers the heart line broadly, including a length subsection; this article targets the specific "short heart line meaning" search intent as a focused companion, following the same pattern as the existing `forked-heart-line-meaning.md`, `chained-heart-line-meaning.md`, and `broken-heart-line-meaning.md` variation articles. `cluster: "line-variations"`, `relatedLesson: "/learn/lines/02-heart-line"`, matching the sibling variation articles and the backlog's original entry.
- Distinct-angle content: a dedicated "Confirming the line is actually short" section teaches readers to distinguish genuine shortness (relative to the palm's own midpoint) from a line that only appears short because it is faint, broken, or obscured by chaining, before any interpretation is offered — a distinction the general `heart-line.md` article does not walk through step by step. The interpretation itself stays within the bound already established in `lessons/lines/02-heart-line.mdx` and `heart-line.md`'s own Length section and FAQ: a shorter line may suggest focused emotional investment rather than limited feeling, and the "short equals cold" inference is explicitly named as a simplification the classical texts do not support. No new palmistry meaning, quotation, or source attribution was introduced beyond what those two existing repository files already establish.
- Sourced from Cheiro (`Palmistry for All`, 1916), William G. Benham (`The Laws of Scientific Hand Reading`, 1900), Fred Gettings (`The Book of the Hand`, 1965), and Peter West (`The Complete Illustrated Guide to Palmistry`, 1998) — the same four Tier 1–3 sources already cited in `heart-line.md`, carried forward rather than re-verified against new claims since no new interpretation was added. No quotation marks are used in the new article.
- Added a single reciprocal navigation link from `heart-line.md`'s existing "Explore further" list to the new article. No other wording in that file changed. Body links to `heart-line.md`, the heart line lesson, `broken-heart-line-meaning.md`, `chained-heart-line-meaning.md`, and `mount-of-venus.md` each appear once, per the internal-linking rule against repeating a link target.
- Verified with `npm run build` (104 page(s) built, including `/blog/beginner/short-heart-line-meaning/`), `npm run content-audit` (59 blog posts, 25 lessons valid), `npm run audit:all` (links, images, schema, indexability, accessibility all pass), and `git diff --check`. Diff is bounded to the new article, the one reciprocal link, and the canonical docs.
- Revision 2 (Director review remediation): the "Length in context" section previously asserted combined readings that no approved repository source establishes as combinations — short line plus depth/faintness, short line plus a Saturn or Jupiter endpoint, short line plus Mount of Venus development, and short line plus a compact or expansive whole-hand pattern. Each has been reduced to separate observation guidance with the shared caution that these variables are held alongside length rather than folded into a length-specific reading, and the two cases where approved sources are silent (Venus mount, whole-hand proportion) now say so explicitly. Three equivalent cross-tradition/consensus overreaches were narrowed in the same pass: an Indian-tradition reading of heart line *length* specifically (approved sources establish *Hridaya Rekha* and *bhava*, not a length reading), "neither the Western nor the Indian tradition reads shortness this way", and "no major tradition Palmistry Path draws from reads length as a relationship tally". The article's structure, SEO intent, observation workflow, navigation, and the core bounded interpretation are unchanged.

## 2026-08-19 - Is Palmistry Real? article published (Relay PP-RELAY-021)
- Added `src/content/blog/beginner/is-palmistry-real.md`, closing the #6-ranked open item in `docs/editorial-backlog.md`'s "Next 10" queue. `beginner/what-palmistry-is.md` and lesson `foundations/01-what-palmistry-is.mdx` already cover what palmistry is and its history; this article targets the distinct skeptic/accuracy search intent ("is palmistry real", "does palm reading work") as an evidence/trust FAQ, not another history-and-definition overview. `cluster: "getting-started"`, `relatedLesson: "/learn/foundations/01-what-palmistry-is"`, matching the backlog's original entry and the pattern already used by `what-palmistry-is.md`, `palmistry-myths.md`, and `can-palm-lines-change.md`.
- Structured around three explicitly separated sections per the task packet: what historical palmists actually claimed (Cheiro's seven-year life-line dating and marriage readings, Benham's six-year divisions and tasselled-ending/islands readings, Varāhamihira's kingship/wealth/longevity marks) stated accurately rather than sanitized; what the evidence does and doesn't establish (no scientific support for occult predictive meaning, no controlled validation of any palmistry tradition, Ray Hyman's 1977 cold-reading account preserved as an anecdote rather than a controlled experiment); and why Palmistry Path still treats palmistry as worth studying (contemplative, cultural, and observational value) despite the second finding. No claim states palmistry has been proven, disproven in a blanket sense, or scientifically validated as predictive; each is explicitly scoped against the overbroad version, matching an existing repo-approved claim pattern.
- Sourced from Cheiro (`Palmistry for All`, 1916), William G. Benham (`The Laws of Scientific Hand Reading`, 1900), Varāhamihira (`Bṛhat Saṃhitā`, 6th c. CE), and Ray Hyman ("Cold Reading: How to Convince Strangers That You Know All About Them", *The Zetetic*, 1977) — all Tier 1–3 sources already established in the site's editorial style guide and the sibling articles it draws from. The one quoted phrase ("from natural causes", attributed to Cheiro) matches wording already verified in `docs/editorial-style-guide.md` §5.2; no new quotation was introduced. No Tier 4 source appears.
- Added a single reciprocal navigation link from `what-palmistry-is.md`'s "What palmistry is not" section to the new article, alongside its existing links to `palmistry-myths.md` and `can-palm-lines-change.md`. No other wording in that file changed.
- Verified with `npm run build` (103 page(s) built, including `/blog/beginner/is-palmistry-real/`), `npm run content-audit` (58 blog posts, 25 lessons valid), `npm run audit:all` (links, images, schema, indexability, accessibility all pass), and `git diff --check`. Diff is bounded to the new article, the one reciprocal link, and the canonical docs.

## 2026-08-19 - Phalanges in Palmistry article published, forked-head-line backlog status corrected (Relay PP-RELAY-020)
- Added `src/content/blog/beginner/phalanges-palmistry.md`, closing the #5-ranked open item in `docs/editorial-backlog.md`'s "Next 10" queue. `finger-shapes-palmistry.md` already carried a brief phalange summary (mental/practical/material framework, sourced to Benham/West/Gettings) as one sub-section among several; this dedicated article goes deeper — the observation method for comparing a finger's three segments to each other (length and thickness/padding, not one finger's tip against another finger's), the terminology and weighting differences between Benham and Gettings, and a synthesis section reading phalange balance alongside each finger's planetary association and joint texture. `cluster: "hand-shape"`, `relatedLesson: "/learn/foundations/03-hand-shapes"`, per the backlog's original entry.
- Sourced from William G. Benham (`The Laws of Scientific Hand Reading`, 1900) and Fred Gettings (`The Book of the Hand`, 1965), the backlog's identified primary sources, with Peter West (`The Complete Illustrated Guide to Palmistry`, 1998) as a supplementary source already used for the same framework in `finger-shapes-palmistry.md`. No quotation-marked wording used — all three sources are paraphrased and attributed, consistent with the site's standing policy that Gettings/West/Fincham quotations cannot currently be verified against their in-copyright editions. No per-finger × per-phalange combination claims were invented beyond what the sources support; the article stays at the level of the general three-register framework and its interaction with each finger's existing planetary meaning, already established elsewhere on the site.
- Added a single reciprocal link each way between the new article and `finger-shapes-palmistry.md`'s existing phalange paragraph — the backlog's own "Internal Link Priorities" table already called for this link. No other existing palmistry claim, quotation, or frontmatter changed in that file.
- Corrected a stale `editorial-backlog.md` status: `beginner/forked-head-line-meaning.md` (Next 10 item #4) was already live on `main`, unrelated to this task, but the backlog table and detail entry still listed it as an open item. Struck through and dated both; the article itself was not touched or rewritten. Item #5 (phalanges) was struck through and dated as newly shipped. The backlog's "Published" count moved from 49 to 50 — only the phalanges article is new; the forked-head-line correction fixes bookkeeping, not a new count.
- Verified with `npm run build` (102 page(s) built, including `/blog/beginner/phalanges-palmistry/`), `npm run content-audit` (57 blog posts, 25 lessons valid), `npm run audit:all` (links, images, schema, indexability, accessibility all pass), and `git diff --check`. Diff is bounded to the new article, the one reciprocal link, and the canonical docs.

## 2026-08-19 - How to Practice Palm Reading article published (Relay PP-RELAY-019)
- Added `src/content/blog/beginner/how-to-practice-palm-reading.md`, closing the #3-ranked open item in `docs/editorial-backlog.md`'s "Next 10" queue. `how-to-read-a-palm.md` teaches the observation sequence itself; this article assumes the reader already knows that sequence and teaches the distinct skill-building intent instead — a repeatable practice routine: rerunning the full sequence before interpreting, working one feature family at a time, recording observations separately from interpretation, comparing the same feature across hands only with permission, revisiting earlier notes after learning more, and naming ambiguity rather than forcing a reading. `cluster: "techniques"`, `relatedLesson: "/learn/foundations/02-how-to-read-a-palm"` (existing route, unchanged).
- Deliberately avoids re-teaching content already covered by two adjacent existing articles rather than compete with them: links to `reading-both-hands-palmistry.md` for the active/passive comparison method instead of repeating it, and to `printable-palmistry-worksheets.md` for a structured recording format instead of reproducing its worksheet layout. No unlaunched product (the planned worksheet pack) is presented as available; the only CTA is the live `/guide/` Starter Guide, matching current article convention.
- Sourced from Fred Gettings (`The Book of the Hand`, 1965) and Johnny Fincham (`The Spellbinding Power of Palmistry`, 2005) for the two palmistry-specific claims made (reading the whole before its parts; training the practitioner's eye before conclusions carry weight). Practice-routine mechanics (one feature family at a time, spaced repetition, revisiting notes) are framed as ordinary skill-building practice, not attributed to palmistry sources, per the task's editorial guardrails.
- Added a single navigation-only reciprocal link from `how-to-read-a-palm.md`'s closing "What comes next" paragraph to the new article. No existing palmistry claim, quotation, or frontmatter in that file changed.
- Updated `docs/editorial-backlog.md`: bumped the published count to 49, struck through and dated item 3 of the Next 10 table, and marked its detail entry published. Updated `docs/CURRENT_STATE.md`'s stale blog post count (53 → 56, the actual current `content-audit` count). Rest of the backlog's Next 10/25/Later queue is unchanged.
- Verified with `npm run build` (101 page(s) built, including `/blog/beginner/how-to-practice-palm-reading/`), `npm run content-audit` (56 blog posts, 25 lessons valid), `npm run audit:all` (links, images, schema, indexability, accessibility all pass), and `git diff --check`. Diff is bounded to the new article, the one reciprocal link, and the three canonical docs.

## 2026-08-19 - Reading Both Hands comparison article published (Relay PP-RELAY-018)
- Added `src/content/blog/beginner/reading-both-hands-palmistry.md`, closing the #2-ranked open item in `docs/editorial-backlog.md`'s "Next 10" queue. `which-hand-to-read-palmistry.md` answers which hand is active/passive; this article assumes that's already settled and teaches the practical comparison workflow instead: aligning both palms, then checking depth/clarity, length, direction/branching, presence/absence, and broad structural agreement across the three major lines. `cluster: "techniques"`, `relatedLesson: "/learn/foundations/04-active-and-passive-hand"` (existing route, unchanged).
- Grounded entirely in already-published source material: the comparison framework and its four core dimensions come from `src/content/lessons/foundations/04-active-and-passive-hand.mdx`'s "Comparing your two hands" section, extended with a second worked example (head line) alongside the existing life-line one. No new source attributions, quotations, or claims beyond what Benham, Gettings, and Cheiro already support in the existing lesson and which-hand article.
- Added a single navigation-only reciprocal link from `which-hand-to-read-palmistry.md`'s "Should you compare both hands?" FAQ answer to the new article. No existing palmistry claim, quotation, or frontmatter in that file changed.
- Updated `docs/editorial-backlog.md`: bumped the published count to 48, struck through and dated item 2 of the Next 10 table, marked its detail entry published, and removed the now-completed reciprocal-link tracking row for `which-hand-to-read-palmistry` → `reading-both-hands-palmistry`.
- Verified with `npm run build` (100 page(s) built, including `/blog/beginner/reading-both-hands-palmistry/`), `npm run content-audit` (55 blog posts, 25 lessons valid), `npm run audit:all` (links, images, schema, indexability, accessibility all pass), and `git diff --check`. Diff is bounded to the new article, the one reciprocal link, and the backlog doc.

## 2026-08-19 - Minor Lines Overview blog pillar published (Relay PP-RELAY-017)
- Added `src/content/blog/beginner/minor-lines-overview.md`, closing the #1-ranked open item in `docs/editorial-backlog.md`'s "Next 10" queue. The article orients beginners to the minor-line layer of the hand — what makes a line "minor," why absence is normal (and why the Mercury Line's absence is explicitly read as favourable while the Sun Line's is not), and how much interpretive weight to give the minor lines relative to the major lines and mounts — then gives a concise, source-supported orientation to each of the four already-published minor-line articles (Sun Line, Mercury Line, marriage/relationship lines, Girdle of Venus) with a link into each for depth. `cluster: "minor-lines"`, `relatedLesson: "/learn/advanced/01-minor-lines-overview"` (existing route, unchanged).
- No new quotation-marked wording was introduced; all claims paraphrase what is already sourced and published on the four dedicated line articles. The unresolved Sun/Mercury quote-fidelity rewrite scoped to `advanced/01-minor-lines-overview.mdx` on `feat/curriculum-wave-3e-3f` was not touched or pulled forward.
- Added a single navigation-only reciprocal link ("For how the [line] fits alongside the other minor lines, see the minor lines overview") to the end of each of the four existing dedicated articles (`sun-line.md`, `mercury-line.md`, `girdle-of-venus.md`, `marriage-relationship-lines.md`). No existing palmistry claim, quotation, or frontmatter in those four files changed.
- Updated `docs/editorial-backlog.md`: bumped the published count to 47, struck through and dated item 1 of the Next 10 table, and marked its detail entry published. The rest of the backlog's Next 10/25/Later queue is unchanged and remains a scored idea list, not an active work queue.
- Verified with `npm run build` (100 routes, including `/blog/beginner/minor-lines-overview/`), `npm run content-audit` (54 blog posts, 25 lessons valid), `npm run audit:all` (links, images, schema, indexability, accessibility all pass), and `git diff --check`. Diff is bounded to the new article, the four reciprocal links, and the backlog doc.

## 2026-08-18 - Lesson `order` collision guard verified, not rebuilt (Relay PP-RELAY-013)
- No code change. The task was authorized to add a per-module duplicate lesson `order` check; inspection showed `scripts/audit-content.mjs` already has one, byte-identical on `main` and on the audited 3E/3F branch, and `ci.yml` already runs `content-audit` on every pull request to `main` and push to `main`. Building a second checker would have duplicated a working guard.
- Verified the existing guard against the exact historical failure: `advanced/simian-line.mdx` and `advanced/04-how-to-give-a-reading.mdx` both at `order: 4` makes `npm run content-audit` exit `1` with `Duplicate order 4 in module "advanced"` naming both files. Also verified a second same-module pair, a three-way collision reporting one error per colliding file, and that the current curriculum — in which four modules legitimately reuse orders `1`–`6` — still passes. All reproductions were made in the working tree and reverted; no fixture curriculum was committed.
- Corrected the stale claim that caused this: `docs/audits/CURRICULUM_WAVE_3E_3F_BRANCH_AUDIT_2026-08.md` §2 carries a correction note, and its open item 3 ("lesson `order` uniqueness has no automated check") is struck through and withdrawn. `CURRENT_STATE.md` now states the guard's contract and CI coverage explicitly, and its lesson count is corrected from 24 to the actual 25.
- Verified with `npm run build` (98 pages), `npm run content-audit`, `npm run audit:all` (links, images, schema, indexability, accessibility), and `git diff --check`. Diff is docs-only plus the Relay result artifact.

## 2026-08-18 - Practice disclosure and module Checkpoint across Mounts (Relay PP-RELAY-012)
- Replayed the already-audited, source-safe Batch 3F `<Practice>` wrapper treatment (`docs/audits/CURRICULUM_WAVE_3E_3F_BRANCH_AUDIT_2026-08.md`) across all eight current Mounts lessons, wrapping each lesson's closing hands-on/takeaway material in `<Practice>` in place of a plain heading or bare paragraph, and importing the existing `src/components/Practice.astro` (unchanged). `01-mounts-overview.mdx`'s former "What you're not doing yet" heading/paragraph is now `<Practice title="Try it: survey the landscape">`, matching the audited branch exactly.
- Added the audited end-of-module Checkpoint to the closing lesson `src/content/lessons/mounts/08-mount-of-mars.mdx`, using the existing `src/components/Checkpoint.astro` and `src/components/CheckpointItem.astro` (unmodified). The four-item `<Checkpoint title="Checkpoint — The Mounts">` block matches the audited branch's wording exactly and recaps only material already established earlier in the module: the seven-planetary-mounts/eight-region arithmetic, the two-part elevation-plus-firmness assessment, and neighbour-relative reading of development.
- One unrelated frontmatter `description` wording change bundled into the audited branch's commit for `08-mount-of-mars.mdx` was intentionally not replayed, following the same precedent as PP-RELAY-010/011's exclusions, because it falls outside the authorized mechanical Practice/Checkpoint scope; that file's description is unchanged from current `main`.
- No lesson wording, frontmatter (other than the excluded description above), routes, ordering, source attribution, quotations, or related-content metadata changed. Verified with `npm run build` (all eight Mounts routes render; Mount of Mars renders four native `<details>/<summary>` checkpoint disclosures; the other seven lessons render the accessible `<section class="practice" aria-labelledby=...>` disclosure natively), `npm run content-audit`, `npm run audit:all` (links, images, schema, indexability, accessibility), and `git diff --check`.

## 2026-08-18 - Practice disclosure and module Checkpoint across Lines (Relay PP-RELAY-011)
- Replayed the already-audited, source-safe Batch 3F `<Practice>` wrapper treatment (`docs/audits/CURRICULUM_WAVE_3E_3F_BRANCH_AUDIT_2026-08.md`) across all six current Lines lessons, wrapping each lesson's closing hands-on/takeaway material in `<Practice>` (or `<Practice title="...">` where the audited branch used a title) in place of a plain `##` heading or bare closing paragraph, and importing the existing `src/components/Practice.astro` (unchanged).
- Added the audited end-of-module Checkpoint to the closing lesson `src/content/lessons/lines/05-fate-line.mdx`, using the existing `src/components/Checkpoint.astro` and `src/components/CheckpointItem.astro` (added by Relay PP-RELAY-010, unmodified here). The five-item `<Checkpoint title="Checkpoint — The Major Lines">` block matches the audited branch's post-correction wording exactly (`ed97caf`, which fixed the fate-line-absence checkpoint answer to stop implying a replacement meaning) and recaps only material already established earlier in the module: line-quality vocabulary and the fork/branch/island distinction from `line-quality-and-markings.mdx`, the absent-fate-line framing from this lesson's own body, and the head/heart-line tension from `03-head-line.mdx`.
- One unrelated frontmatter `description` wording change bundled into the audited branch's commit for `line-quality-and-markings.mdx` was intentionally not replayed, following the same precedent as PP-RELAY-010's `thumb-and-fingers.mdx` exclusion, because it falls outside the authorized mechanical Practice/Checkpoint scope; that file's description is unchanged from current `main`.
- The pre-existing life-line testing-claim wording in `04-life-line.mdx` ("it has not held up under testing") was left untouched, as required by the task packet.
- No lesson wording, frontmatter (other than the excluded description above), routes, ordering, source attribution, quotations, or related-content metadata changed. Verified with `npm run build` (all six Lines routes render; Fate Line's checkpoint renders five native `<details>/<summary>` disclosures; the other five lessons render the accessible `<section class="practice" aria-labelledby=...>` disclosure natively), `npm run content-audit`, `npm run audit:all` (links, images, schema, indexability, accessibility), and `git diff --check`.

## 2026-08-18 - Practice disclosure and module Checkpoint across Foundations (Relay PP-RELAY-010)
- Replayed the already-audited, source-safe Batch 3F `<Practice>` wrapper treatment (`docs/audits/CURRICULUM_WAVE_3E_3F_BRANCH_AUDIT_2026-08.md`) across all five Foundations lessons, wrapping each lesson's closing hands-on/takeaway section in `<Practice title="...">` in place of a plain `##` heading and importing the existing `src/components/Practice.astro` (unchanged).
- Added `src/components/Checkpoint.astro` and `src/components/CheckpointItem.astro`, reused unmodified from the audited branch version: stateless, presentational components with no client JS, scoring, or persistence, using native `<details>/<summary>` for keyboard-accessible answer disclosure.
- Added the audited end-of-module Checkpoint to the closing lesson `src/content/lessons/foundations/04-active-and-passive-hand.mdx`, matching the audited branch treatment exactly. Its four prompts recap only material already established earlier in the current Foundations module (observation order, the two shape-classifying inputs versus the three qualifying observations from `03-hand-shapes.mdx`, thumb opening/finger length from `thumb-and-fingers.mdx`, and the active/passive framing from this lesson) — verified against current `main` content, not assumed from the audit.
- One unrelated wording change bundled into the audited branch's commit — a frontmatter `description` tweak in `thumb-and-fingers.mdx` — was intentionally not replayed because it falls outside the authorized mechanical Practice/Checkpoint scope; that file's description is unchanged from current `main`.
- No lesson wording, frontmatter (other than the excluded description above being left alone), routes, ordering, source attribution, quotations, or related-content metadata changed. Verified with `npm run build` (all five Foundations routes render; the closing lesson's checkpoint renders four native `<details>/<summary>` disclosures), `npm run content-audit`, `npm run audit:all` (links, images, schema, indexability, accessibility), and `git diff --check`.

## 2026-08-18 - Practice disclosure on two Advanced lessons (Relay PP-RELAY-009)
- Replayed the already-audited, source-safe Batch 3F `<Practice>` wrapper treatment (`docs/audits/CURRICULUM_WAVE_3E_3F_BRANCH_AUDIT_2026-08.md`) onto `src/content/lessons/advanced/02-marriage-relationship-lines.mdx` and `src/content/lessons/advanced/03-girdle-of-venus.mdx`, the two remaining Advanced lessons the audit classified as mechanical. Each lesson's closing "What to take away from your hand" section is now wrapped in `<Practice title="What to take away from your hand">` in place of a plain `##` heading, importing the existing `src/components/Practice.astro` (landed unmodified by Relay PP-RELAY-008) with the same relative import path already used by `combining-what-you-see.mdx`.
- No lesson wording, frontmatter, routes, ordering, source attribution, quotations, or related-content metadata changed; the diff against the audited branch treatment is byte-identical for both files. `Practice.astro` itself was not touched.
- Verified with `npm run build` (both `/learn/advanced/02-marriage-relationship-lines/` and `/learn/advanced/03-girdle-of-venus/` render the accessible `<section class="practice" aria-labelledby=...>` disclosure natively), `npm run content-audit`, `npm run audit:all` (links, images, schema, indexability, accessibility), and `git diff --check`.

## 2026-08-18 - Combining What You See synthesis lesson (Relay PP-RELAY-008)
- Replayed the already-audited, source-safe Batch 3F *Combining What You See* synthesis lesson (`docs/audits/CURRICULUM_WAVE_3E_3F_BRANCH_AUDIT_2026-08.md`) onto current `main` as a fresh bounded implementation, per that audit's explicit recommendation, rather than merging the stale `feat/curriculum-wave-3e-3f` branch. Only this one lesson was replayed; the rest of Batch 3F (the capstone body revision, module `<Checkpoint>`s, and the Sun/Mercury quote-fidelity rewrite in `advanced/01-minor-lines-overview.mdx`) remains deliberately out of scope.
- Added `src/content/lessons/advanced/combining-what-you-see.mdx`, route `/learn/advanced/combining-what-you-see/`, `order: 5` — immediately after the Simian Line and before the capstone. `advanced/04-how-to-give-a-reading.mdx` moved from `order: 5` to `order: 6` to stay collision-free; only its `order` field changed, not its body. No blank `relatedArticle` value was introduced; the field is simply omitted, matching current `main`'s convention for lessons without a natural related article.
- The lesson teaches a five-stage synthesis method (observation, weighting, agreement, contradiction, active/passive comparison) and closes the curriculum audit's previously identified P0 gap — no prior lesson demonstrated a complete worked reading. It reuses only associations already established elsewhere in the curriculum, introduces no new palmistry doctrine, and repeatedly enforces that repetition across features increases a theme's weight *within the traditional reading*, not evidentiary certainty about the person. Closes with an explicit statement of what the course does not address (career, relationships, health, lifespan) and that the person remains the authority on whether a reading resonates.
- Added `src/components/Practice.astro`, reused unmodified from the audited branch version: a stateless, presentational section wrapping a lesson's closing hands-on exercise, with no client JS, scoring, or persistence, and a scoped heading id for its accessible name.
- Core curriculum is now 25 lessons — the approved target reached in full. Advanced is now 6 lessons. Verified with `npm run build`, `npm run content-audit`, `npm run audit:all`, and `git diff --check`.

## 2026-08-18 - Blank relatedArticle cleanup (Relay PP-RELAY-007)
- Removed the blank `relatedArticle: ""` frontmatter field from the two advanced lessons that had carried it as an unused legacy value: `src/content/lessons/advanced/01-minor-lines-overview.mdx` and `src/content/lessons/advanced/04-how-to-give-a-reading.mdx`. The field is optional (`z.string().optional()` in `src/content.config.ts`); no related article was invented to fill it.
- This eliminates the two "relatedArticle is blank; optional legacy value ignored" warnings that `npm run content-audit` had reported as pre-existing since Relay PP-RELAY-004/005/006 (see prior changelog entries); the audit now passes with zero warnings.
- No title, slug, route, lesson ordering, body copy, palmistry claims, or indexability behavior changed. Verified with `npm run content-audit`, `npm run build`, and `git diff --check`.

## 2026-08-17 - Frontmatter description-length cleanup (Relay PP-RELAY-006)
- Shortened the `description` frontmatter field on the four lesson files the baseline `npm run content-audit` flagged for exceeding the 170-character SEO target: `src/content/lessons/advanced/simian-line.mdx`, `src/content/lessons/foundations/thumb-and-fingers.mdx`, `src/content/lessons/lines/line-quality-and-markings.mdx`, and `src/content/lessons/mounts/08-mount-of-mars.mdx`.
- Each revised description trims the existing wording only (no new palmistry claim, source attribution, or search-intent change) and stays within the audit's accepted length.
- No title, slug, route, lesson ordering, body copy, or indexability behavior changed. Verified with `npm run content-audit`, `npm run build`, and `git diff --check`; the two remaining content-audit warnings (blank `relatedArticle` on two advanced lessons) are pre-existing and out of this task's scope.

## 2026-08-17 - Simian Line module move (Relay PP-RELAY-005)
- Replayed the already-approved, independently audited Batch 3E Simian Line module move (from the unmerged `feat/curriculum-wave-3e-3f` branch audit, `docs/audits/CURRICULUM_WAVE_3E_3F_BRANCH_AUDIT_2026-08.md`) onto current `main` as a fresh bounded implementation, rather than merging that stale branch. The Sun/Mercury quote-fidelity rewrite and all 3F synthesis/practice work remain deliberately out of scope.
- Moved `src/content/lessons/lines/06-simian-line.mdx` to `src/content/lessons/advanced/simian-line.mdx`, route `/learn/advanced/simian-line/`. It is now the fourth lesson of Minor Lines & Synthesis (`order: 4`, `difficulty: intermediate`, matching the module); `advanced/04-how-to-give-a-reading.mdx` moved to `order: 5` to stay collision-free. The Lines module is now 6 lessons and Advanced is 5; the core curriculum is still 24 lessons total.
- Replaced the lesson's now-false "these quality distinctions apply to all major lines in this module" self-reference (no longer true once the lesson left the Lines module) with a link to the existing `lines/line-quality-and-markings` lesson. No palmistry interpretation, source framing, or quotation changed.
- The old `/learn/lines/06-simian-line/` URL keeps resolving through a new static stub (`src/pages/learn/lines/06-simian-line.astro`): `noindex, follow`, canonical to the new route, a `0;url=` meta-refresh to the new route, and `data-pagefind-ignore`. Added it to the `NOINDEX_PATHS` allowlist in `src/indexability.mjs` so it is excluded from the sitemap and Pagefind like the site's other utility routes. `scripts/audit-schema.mjs` now treats any noindex-allowlisted route as exempt from the lesson-page `LearningResource`/`BreadcrumbList` schema requirement, since a redirect stub is not a lesson page; this reuses the existing indexability allowlist rather than adding a bespoke list.
- Added an optional `canonicalPath` prop to `src/components/BaseHead.astro` (defaults to the current page's own path, unchanged for every other caller) so the stub page can canonicalize to a different route than its own URL.
- Repointed the three direct internal references to the old lesson URL: `simian-line.md`'s `relatedLesson`, the inline link in `m-line-palmistry.md`, and the glossary's "Simian line" entry in `src/pages/glossary.astro`. Updated the Lines and Advanced module descriptions in `src/consts.ts` to match the new module contents.
- Corrected a stale `CURRENT_STATE.md` note that described Relay PP-RELAY-004's thumb-article fix as living on an unmerged branch; it merged into `main` via PR #17 on 2026-08-17.
- Verified with `npm run build`, `npm run content-audit`, and `npm run audit:all` (link, image, schema, indexability, accessibility), all passing; confirmed both `/learn/lines/06-simian-line/` and `/learn/advanced/simian-line/` are emitted, the old route is absent from `dist/sitemap*.xml` and the Pagefind index, and no unintended internal reference to the old URL remains.

## 2026-08-17 - Thumb article Cheiro/Benham and thumb-length fixes (Relay PP-RELAY-004)
- Corrected two of the three open defects in `src/content/blog/beginner/thumb-meaning-palmistry.md` recorded in `ACTIVE_TASK.md` from Remediation Batch 3D: the will/logic framework was misattributed to Cheiro as well as Benham, and an unsupported thumb-length threshold ("reaches to or beyond the first joint" of the index finger) appeared in neither cited author.
- Will and logic are now attributed to Benham alone, with Cheiro's own second-phalange reading described as tact and diplomacy per *Palmistry for All* rather than logic; Cheiro's verified quotation on the thumb's overall importance is preserved.
- The thumb-length threshold now uses the same source-verified Benham landmark and setting caveat already taught in `src/content/lessons/foundations/thumb-and-fingers.mdx`: ordinary length reaches roughly the middle of the index finger's base segment, with a note that thumb setting can confound the comparison.
- The unresolved Fred Gettings quotation issue in the same article was explicitly left out of scope, per the authorized task packet.
- No lesson, route, schema, dependency, or SEO behavior changed. Verified with `npm run build`, `npm run content-audit`, and `git diff --check`.

## 2026-08-17 - Curriculum Wave 3E/3F branch audit (Relay PP-RELAY-003, revision 2)
- Independently audited the unmerged remote branch `feat/curriculum-wave-3e-3f` (tip `c70f861`) against current `main` (`0abbf75`): full file-by-file scope classification, source/provenance review of the new sun/Mercury quotations, the simian-lesson move/redirect, merge-conflict analysis, and a clean `npm ci` / `build` / `content-audit` / `audit:all` run in an isolated worktree. Findings recorded in `docs/audits/CURRICULUM_WAVE_3E_3F_BRANCH_AUDIT_2026-08.md`.
- The branch stays inside the approved 3E/3F scope and validates cleanly. Every curriculum/runtime file merges with no textual conflicts, but revision 2 corrects an inventory/merge-feasibility error in the first pass: five `docs/*.md` files overlap and produce real Git conflict markers (`ACTIVE_TASK.md`, `AI_HANDOFF.md`, `CHANGELOG.md`, `CURRENT_STATE.md`, `ROADMAP.md` — `ROADMAP.md` was omitted from the original table), not just narrative drift as first reported. Two bounded items remain before shipping: a source-fidelity check on the new Cheiro/Benham quotations in `advanced/01-minor-lines-overview.mdx`, and conflict resolution plus editorial reconciliation of those five docs files against current `main`'s narrative.
- Replaced the "uninspected/unverified" placeholder statements about the branch in `ACTIVE_TASK.md` and `AI_HANDOFF.md` with these objective findings.
- No curriculum, runtime, dependency, or SEO behavior changed; this Relay diff is documentation-only and the audited branch itself was not modified, merged, or rebased.

## 2026-08-16 - CI validation workflow (Relay PP-RELAY-002)
- Added `.github/workflows/ci.yml`, a general-purpose validation workflow that runs on pull requests targeting `main` and on pushes to `main`. It checks out the repo, installs Node 22 dependencies from the committed lockfile with `npm ci`, and runs `npm run build`, `npm run content-audit`, and `npm run audit:all` (link, image, schema, indexability, and accessibility audits). Permissions are `contents: read`; runs are grouped and cancelled per PR/ref to avoid wasting resources on superseded pushes.
- Ordinary PRs and pushes to `main` now get the same build/audit coverage the Relay pilot already used for its own dispatched tasks; previously only `.github/workflows/relay-dispatch.yml` existed, and it only fires the Relay routine on `.ai-ops/state.json` changes.
- `relay-dispatch.yml` was not modified.
- No dependency, content, or runtime behavior changed; verified locally with a clean `npm ci` followed by `npm run build`, `npm run content-audit`, and `npm run audit:all`, all passing.

## 2026-08-16 - Documentation reconciliation (Relay PP-RELAY-001)
- Reconciled `CURRENT_STATE.md`, `ACTIVE_TASK.md`, and `AI_HANDOFF.md` against verified `main` history: Curriculum Batches 3A–3D (PRs #8–#11) and the Technical Remediation Wave (PR #12) were previously documented as pushed-but-unmerged and are confirmed merged into `main` as of this date.
- Added the Technical Remediation Wave (accessible 404 page, expanded blog+lesson `content-audit` coverage, homepage PNG→WebP conversion) to `ROADMAP.md` and `CURRENT_STATE.md`; it had not previously been recorded in canonical state docs.
- Noted that `feat/curriculum-wave-3e-3f` exists on the remote unmerged and unreviewed; not treated as shipped.
- No runtime, content, or SEO behavior changed.

## 2026-08-13 - Remediation Batch 3D the thumb and the fingers
- Added **The Thumb and the Fingers** as the fourth lesson of Foundations (`src/content/lessons/foundations/thumb-and-fingers.mdx`, route `/learn/foundations/thumb-and-fingers`). It teaches the structural pass the course previously walked past: thumb length against a source-verified landmark, thumb setting, the two thumb sections, resting opening angle, tip-joint flexibility, the one-time planetary finger mapping, per-finger comparison landmarks, base arch, resting spacing, and lean. The core curriculum is now **24 lessons**; Foundations has **5**.
- The new lesson carries an unnumbered filename with `order: 4`; `04-active-and-passive-hand.mdx` moved to `order: 5` and remains the Foundations closing lesson. **No file renamed and no URL changed.**
- **Thumb length is now taught from a verified landmark.** Benham (1900) gives it: a thumb of ordinary length reaches about the middle of the index finger's base segment, with the thumb held against the side of the hand — plus his own caveat that a low or high setting confounds the measurement. Cheiro gives no landmark at all; the ambiguous "reaches to or beyond the first joint" wording in the blog is not his and is not reproduced.
- **Will and reason are taught in three separated moves**: the physical proportion, then the historical reading, then the site's boundary. Benham's own framing is quoted — he adopted the two names "for brevity" — and the lesson states plainly that they are traditional labels for a proportion, not measurements of anyone's intelligence, willpower, self-control, judgement, or character. **No invented modern meaning replaces the historical one.**
- Deliberately out of core: the clubbed or "murderer's" thumb and all sensational variants, thumb-tip and fingertip typologies, the three phalange zones, knotted-versus-smooth joints, per-finger meaning tables, finger flexibility as a separate test, and nails. Nails carry a one-sentence boundary that does not imply a future lesson. No Indian or Chinese equivalence is claimed; the lesson states that the will/reason frame and the planetary finger names are Western constructions.
- **Finger prominence is not raw length.** The middle finger is longest on almost every hand, so the lesson teaches each finger against Benham's own per-finger landmarks and defines prominence as departure from those, not as being longer than a neighbour.
- Retitled `foundations/03-hand-shapes.mdx` to **Hand Shape, Texture, and Flexibility** (route unchanged, duration 8→10) and added skin texture, consistency under pressure, and whole-hand flexibility as a **separate qualifying layer placed after the four types**. The elemental classification still rests on two inputs and only two; the section says so explicitly and states that these observations never change a hand's type. Boundaries are stated on the page: texture is not line quality, consistency is not mount firmness, whole-hand flexibility is not the thumb-tip joint.
- Added the historical clarification that d'Arpentigny (1843) supplied the earlier Western shape typology, that the four-element framing came later, and that **Cheiro and Benham did not use the system this lesson teaches**.
- Revised `foundations/02-how-to-read-a-palm.mdx`: step three compressed to an observation prompt now that lesson 3 teaches the interpretations; a new thumb/finger step added so the course's own reading sequence no longer omits a layer it teaches (steps renumbered to seven); the claim that palm-to-finger ratio is "the foundation of every major hand classification system" narrowed to what the repo supports; "associations across traditions" removed; and the Sun/Apollo inversion corrected to match the Mounts module.
- Corrected the forward reference in `mounts/03-mount-of-jupiter.mdx`, which pointed at "the finger lessons that follow" — after 3D that lesson exists and precedes the Mounts module.
- Repointed `relatedLesson` in `thumb-meaning-palmistry.md` and `finger-shapes-palmistry.md` to the new lesson. The new lesson's `relatedArticle` is the finger article, chosen because it carries no quotation-marked source passages; the thumb article's unresolved Gettings quotation remains a separate backlog item.
- Adopted the **feature-branch review gate**: agents may implement, test, commit, and push on task branches without a separate approval step. Merging to `main` still requires explicit user review. Recorded in `AGENTS.md` and `CLAUDE.md`.

## 2026-08-11 - Remediation Batch 3C line quality and markings
- Added **Line Quality and Markings** as the first lesson of the Lines module (`src/content/lessons/lines/line-quality-and-markings.mdx`, route `/learn/lines/line-quality-and-markings`). It teaches the observational vocabulary shared by every line — depth as a continuum, clarity as a separate axis, relative rather than absolute depth, clean/chained/broken continuity, clean versus overlapping breaks, forks versus branches, islands, and parallel/support lines — and teaches description before interpretation. The core curriculum is now **23 lessons**; the Lines module has **7**.
- The new lesson carries an unnumbered filename. Lesson URLs derive from the filename while display order derives from the `order` frontmatter, so the new lesson takes `order: 1` and the six existing Lines lessons increment to 2–7 with **no file renamed and no URL changed**. The 45 inbound links to the existing Lines routes are unaffected.
- Made the four major-line lessons subtractive rather than additive. The generic depth/faint/chained definitions, the relative-depth rule, and the generic break, fork, branch, and parallel-line definitions were removed from `lines/02`–`05`, which had quadruplicated them; each lesson now applies the shared vocabulary to its own line instead of re-deriving it. Net 30 insertions against 36 deletions.
- Kept all line-specific historical and source material intact, including Benham's nervous-versus-muscular vitality distinction on the life line, the Line of Mars quotations, the classical-versus-modern life-line break discussion, and the Batch 2B lifespan corrections. Added a brief physical definition of a tasselled ending inside the existing Benham passage in `lines/04` without softening the mortality claim it reports.
- Islands are taught as form, then the verified Cheiro (1916) and Benham (1900) health readings quoted exactly, then Palmistry Path's own boundary. **No invented modern universal meaning replaces the historical reading** — the site's contemporary position is stated as methodological (which line, where, how large, surrounding condition, neighbouring features, other hand), and the asymmetry is stated openly on the page.
- Deliberately out of scope for the new lesson: crosses, stars, grilles, triangles, squares, tasselled endings, and the line-specific variables of length, arc, slope, and endings. The lesson states both boundaries explicitly.
- No new inline "go deeper" linking convention was introduced; the lesson uses `relatedArticle` only. No figure was removed — each existing plate carries its line's name in the artwork and retains line-specific teaching value. `lines/06-simian-line.mdx` changed in frontmatter `order` only.

## 2026-08-11 - Remediation Batch 3B truthful labelling, mount model, simian correction
- Corrected the live source-integrity defect in `lines/06-simian-line.mdx`, which still asserted *ekaagra* as Hasta Samudrika Shastra doctrine after Batch 2B corrected exactly that framing in the twin article. The lesson now presents *ekāgratā* as genuine yogic/philosophical terminology whose application to the single transverse crease is a modern cross-tradition reading. The same stale attribution in `heart-line.md` was corrected to match, and the file's internal 1–3% / 1–4% prevalence contradiction was resolved to 1–3%.
- Published one mount model across the site: seven planetary mounts occupy eight mount regions, because Mars appears in two distinct areas of the palm. The Plain of Mars is a separate central region, not a raised mount and not a ninth mount. The mounts overview lesson explains the count once; `foundations/02` now names all eight regions explicitly.
- Fixed a factual error in the glossary, which equated the Outer Mount of Mars with the Plain of Mars.
- Made module labelling truthful. Module 4 was titled "Advanced Interpretation" and labelled Advanced while containing four intermediate lessons on minor lines and synthesis; it is now "Minor Lines & Synthesis", Intermediate. The `advanced` slug and every lesson URL are unchanged. Module 2 became "The Major Lines" and no longer promises Sun, Mercury, and Girdle content that lives in module 4. Module difficulty labels now match the lessons they contain.
- Stated briefly in `foundations/02` that the course teaches line reading before full mount assessment even though a complete reading assesses the mounts first — the two orders differ deliberately.
- Removed the decorative `prerequisites` frontmatter field from all 22 lessons and from the content schema. It restated the previous lesson, had zero code consumers, and was never rendered.
- No lesson was added, moved, or renamed; no route changed; no pricing, waitlist, checkout, or product promise was touched.

## 2026-08-11 - Remediation Batch 2C quotation integrity
- Closed the 78-item quotation backlog from Batch 2B. Re-inventorying from source found 245 quotation-marked passages inside paragraphs naming Cheiro or Benham, of which 162 were genuine attributions: 55 already exact, 107 corrected, none left unverified.
- Corrected five attributions that carried a claim the source does not make: Benham does not read an absent Mercury/health line as a sign of a sound constitution (that is Cheiro's reading); Benham offers no reassurance about a missing Sun Line, and Cheiro says close to the opposite; Benham reads a fate line from Luna as help from one of the opposite sex, not from "the public"; Benham argues for "affection" over "union" rather than against it; and "writer's fork" is in neither author.
- Corrected the wrong work on Mars: Cheiro's mount-by-mount treatment is in *Cheiro's Language of the Hand*, not *Palmistry for All*.
- Corrected the Batch 2B replacement quotation for the active/passive hand. The wording is in *Language of the Hand* but belongs to a reader's letter signed "Speranus", not to Cheiro. Cheiro states the principle himself in *Palmistry for All*, and that is now quoted.
- Re-attributed the sister line: "great vitality and power of resistance to illness" is Cheiro's, not Benham's, though Benham does treat the line and his own wording now carries the point.
- Recorded that fourteen passages matched the corpus and were still wrong, most of them short phrases attached to the wrong feature — string match is not verification.

## 2026-08-10 - Batch 2B final consistency sweep
- Removed leftover prose that still contradicted the corrected life-line framing further down the same pages: the empirical section's appeal to the classical writers, the "does not count out years" claim, the Short and Broken variation framings, and the short/broken FAQs.
- Rewrote the life-line lesson's break section as classical (Benham illness/accident, Cheiro danger of death) versus modern practice versus Palmistry Path policy, and updated the figure captions to match.
- Corrected the same class of claim in `short-life-line-meaning.md`, `broken-life-line-meaning.md`, `m-line-palmistry.md`, `palmistry-myths.md`, and `heart-line.md`.
- Qualified the *Jeevan Rekha*/*prana* framing as modern Hindi terminology, and replaced "standard in current practice" for the Tian/Di/Ren line mapping with wording tied to the material actually consulted.
- Corrected the style guide's "Taoist" description of *Tian Di Ren* and its unverified claim that relationship predictions are not grounded in any major classical tradition.

## 2026-08-10 - Batch 2B pre-merge correction pass
- Corrected the life-line historical framing across nine content files, the glossary, and the print reference. Cheiro and Benham both read the life line for length of life and both supplied dating systems; the site no longer claims the tradition rejected lifespan reading. Palmistry Path's non-predictive stance is now presented as a modern editorial position resting on evidence.
- Added a durable editorial rule separating historical description from Palmistry Path's own boundary, so the tradition is never sanitised to match site policy.
- Re-audited the Chinese article: removed line-variation interpretations that rested only on Tier 4 material, reframed remaining line readings as contemporary practice, corrected the "interpretive, not prophetic" claim, and stopped claiming the lifespan reading was absent from the classical teaching of either tradition.
- Re-audited the Indian article: removed the claim that no Indian tradition predicts outcomes with certainty (the *Bṛhat Saṃhitā* is openly predictive), and marked the karmic fate-line reading, mount correspondences, joint chart/hand practice, and per-formation meanings as contemporary practice or removed them.
- Fixed two Tian/Di/Ren mapping errors in the Lines lessons and standardised the mapping and its source status across all occurrences.
- Softened the About page sourcing claim to match the four-tier policy, and stated that describing the tradition honestly means reporting predictive claims the site does not endorse.

## 2026-08-10 - Remediation Batch 2B source integrity and editorial trust
- Added a permanent task startup/local sync procedure to `AGENTS.md` so agents fetch, update `main`, and branch without manual user intervention.
- Replaced the editorial style guide's source section with a four-tier source policy, plus rules for tradition-specific sourcing, quotation integrity, and encyclopaedia handling. Removed the prior allowance for citing commercial astrology sites.
- Removed every commercial astrology blog, SEO content farm, and unsourced blog from citations and body prose across nine articles, and deleted the editorial vouching for a commercial palmistry blogger.
- Replaced Wikipedia and Britannica body-prose attributions with the underlying sources they summarise.
- Gave the Chinese and Indian tradition articles tradition-appropriate sourcing (Kohn 1986, Smith 1991, Zysk 2016, Varāhamihira), corrected `san cai` from "Taoist" to classical Chinese cosmology, removed the Vedic-scripture framing, and marked the modern Hindi *rekha* names and the Heaven/Human/Earth mapping as contemporary practice rather than classical doctrine.
- Corrected or qualified factual claims that failed verification: Romani arrival dating, the caduceus, simian line prevalence, "every inhabited continent", the *ekāgratā* attribution, and Cheiro biography inconsistencies.
- Verified 92 author-attributed quotations against the public-domain primary texts; corrected the highest-risk failures, including the life-line lifespan quotation used in five places and the claim that Cheiro and Benham read life-line breaks as transition markers.
- Regenerated the About page source list from the corpus the site actually cites.
- Added `docs/source-verification-log.md` as the durable record of what was verified and what remains open.

## 2026-08-10 - Remediation Batch 2A accessibility and UX safety
- Added global skip-to-main navigation and a shared `:focus-visible` treatment.
- Added reliable `main#main-content` targets across public layouts/pages.
- Improved muted-text contrast by reducing opacity stacking in shared navigation, footer, lesson, blog, guide, and waitlist surfaces.
- Replaced Lines lesson Figure prompt-style alt text with learner-facing descriptions.
- Increased practical lesson-path and header navigation target sizes while preserving the visual design.
- Added visible email labels, described-by relationships, and live/status semantics to the Starter Guide and Complete Reference waitlist forms.
- Added browse fallback content on `/search/` and changed `/learn/` module card titles from paragraphs to headings.
- Added a focused accessibility audit for skip-link/main-target and prompt-like Figure alt regressions.

## 2026-08-10 - Remediation Batch 1 trust and technical safety
- Put the Complete Reference into waitlist-only state and removed the dead paid checkout URL from user-facing flows.
- Preserved the Complete Reference PDF source outside public Astro routing so `/print/complete-reference/` is no longer emitted.
- Temporarily disabled AdSense by leaving the publisher ID empty.
- Added centralized sitemap/Pagefind/noindex policy for private and utility routes.
- Added a generated-output audit for private route exposure, sitemap/Pagefind exclusions, AdSense script emission, and the dead Gumroad checkout URL.
- Applied conservative dependency remediation with `npm audit fix`; remaining advisories require a breaking Astro/sharp upgrade path.

## 2026-08-09 - Agent operating-system migration
- Added shared `AGENTS.md` workflow with progressive disclosure, Git safety, risk-based validation, active-task memory, definition of done, and cross-agent review rules.
- Reworked `CLAUDE.md` around Opus/Sonnet/Haiku routing, Codex independent review, token discipline, and `/clear`/`/compact` boundaries while preserving the established article approval workflow.
- Replaced monolithic `PROJECT.md` startup behavior with a compatibility pointer.
- Added canonical wiki pages for product vision, current state, architecture, roadmap, decisions, ideas, handoff, and active task.
- Preserved existing specialized editorial, SEO, email, visual, launch, and worksheet documents as targeted references.
- Replaced the Astro starter README with a Palmistry Path-specific project README.

No runtime/site behavior was intentionally changed by this migration.
